# Materials Used — Reference-first mimic test v0.1

Created: 2026-06-12

## Fonts (downloaded locally, self-hosted — no runtime CDN)

| File | Family | Source | License |
|---|---|---|---|
| `assets/fonts/BigShouldersDisplay-800-latin.woff2` | Big Shoulders Display 800 | Google Fonts (fonts.gstatic.com), latin subset | SIL Open Font License 1.1 (free, embeddable) |
| `assets/fonts/Fraunces-italic-400-latin.woff2` | Fraunces italic 400 (opsz 9pt) | Google Fonts (fonts.gstatic.com), latin subset | SIL Open Font License 1.1 |
| `assets/fonts/Fraunces-italic-600-latin.woff2` | Fraunces italic 600 (opsz 9pt) | Google Fonts (fonts.gstatic.com), latin subset | SIL Open Font License 1.1 |

- Both families are OFL-1.1, which permits embedding/redistribution. Files were
  downloaded once into this experiment folder and referenced via local `@font-face`,
  so the page has **no runtime CDN / external font dependency**.
- Only the latin subset was downloaded (small files), enough for the English copy.

## Images / textures

- **None downloaded.** All texture is self-authored:
  - Paper grain: inline SVG `feTurbulence` filter (self-made).
  - The only "image" is a self-made generative halftone canvas (vanilla JS,
    seeded) — no external image assets.

## Copy / content

- All wording is original and fictional (a made-up risograph press / show).
- No reference site text, logos, colours, or layout were copied.

## Boundaries honored

- Worked only inside `~/CreativeVault/70_design_experiments/reference_first_mimic_test_v0_1/`.
- No MedicalVault / LocalOnlyProjects / other vaults. No secrets / API / tokens / .env.
- No existing files overwritten. No external code pasted. No reference site copied.
</content>
